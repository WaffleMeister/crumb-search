class QueryParser:

    @staticmethod
    def parse_search_query(search_query):
        ''' This class takes in a search query, and parses it out to 
            a set of trigrams the rest of the program can support.

            Supported regular expressions:

            *
            +
            ?
        '''

        return NotImplementedError